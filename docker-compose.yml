version: '3.8'

services:
  backend:
    build: ./backend
    container_name: ccp-backend
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production

  frontend:
    build: ./frontend
    container_name: ccp-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
